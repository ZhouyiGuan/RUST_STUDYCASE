/**************************************************
注释可以进行测试，格式如下
**************************************************/

/// `add_one` 将指定值加1
///
/// # Examples
/// ```
/// let arg = 5;
/// let answer = genernal::add_one(arg);
///
/// assert_eq!(6, answer);
/// ```
pub fn add_one(x: i32) -> i32 {
    x + 1
}

fn main(){}